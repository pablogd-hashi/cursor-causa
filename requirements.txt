# Causa orchestrator + console.
pydantic>=2,<3
pyyaml==6.0.2

# API (Phase 4)
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Console (Phase 5)
streamlit==1.41.1
requests==2.32.3

# MCP: used for both CAUSA_TRIAGE=mcp triage and the Causa MCP server
# (causa/mcp_server.py) that exposes investigation tools to Cursor.
mcp>=1.2.0
httpx>=0.27.0
