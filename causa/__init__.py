"""Causa — a Cursor-driven Root Cause Analysis console.

Phase 0 ships only the RCA contract (``causa.contract``). Later phases add the
triage source adapters, the topology source, the investigator interface, the
FastAPI app, and the Streamlit console.
"""

__version__ = "0.0.0"
